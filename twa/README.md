# Android TWA (Trusted Web Activity)

## Goal

List Baby Tracker on the Google Play Store without maintaining a separate
native codebase, by wrapping the deployed PWA in a
[Bubblewrap](https://github.com/GoogleChromeLabs/bubblewrap)-generated
Trusted Web Activity — a thin Android shell that just opens the real web app.

The TWA also gets Play Billing wired up (`features.playBilling.enabled:
true` in `twa-manifest.json`), which exposes the
[Digital Goods API](https://developer.chrome.com/docs/android/trusted-web-activity/receive-payments-play-billing)
to the web app. `src/hooks/useDigitalGoods.ts` and `src/hooks/useEntitlement.ts`
use that to query/purchase entitlements — the intended end state is gating
**some features to Play Store buyers only**. Which features is a future
product decision, not decided yet — nothing is gated today.

This covers Android/Play only. There is no equivalent purchase API for iOS —
Apple requires StoreKit inside a native app for in-app purchases, which is a
separate, much larger project (e.g. wrapping the PWA with Capacitor) than
this TWA scaffold. `useEntitlement()` returns `owned: false` with a
`supported: false` reason everywhere except the Android TWA, and does not
attempt to gate anything on iOS.

## TODO

Nothing below has been done yet — this folder is a scaffold, not a working
build. In order:

- [ ] Deploy the app and have a real production domain.
- [ ] Fill in `twa-manifest.json`: replace every `REPLACE_WITH_YOUR_DOMAIN`,
      and double check `packageId` (`com.mikkvillem.babytracker`) — it can't
      change after the app is published to Play.
- [ ] Generate the Android project + a release signing key (`npm run
      twa:build` from the repo root, or `bubblewrap build` from this
      directory). Back the keystore up somewhere safe outside the repo.
- [ ] Get the SHA-256 cert fingerprint and put it in
      `public/.well-known/assetlinks.json` (repo root) alongside the real
      `package_name`, then redeploy the web app and verify with Google's
      [Digital Asset Links tool](https://developers.google.com/digital-asset-links/tools/generator).
- [ ] Create a Play Console listing, upload the release bundle, decide which
      feature(s) to gate, and create matching products in Play Console
      (Monetize → Products) — the SKUs used from the app must match.
- [ ] Wire `useEntitlement('<sku>')` into whichever feature gets gated.

Full detail on each step is in Setup below.

## Setup

1. **Deploy the app** and note its real domain (`npm run deploy`).

2. **Fill in the domain** in `twa-manifest.json`: replace every
   `REPLACE_WITH_YOUR_DOMAIN` with the real host (no protocol for `host`,
   full `https://` URLs elsewhere).

3. **Generate the Android project and signing key.** With JDK 17+ and the
   Android SDK available (Bubblewrap can install both on first run):

   ```sh
   cd twa
   npx @bubblewrap/cli build
   ```

   The first `build` run will offer to generate `android.keystore` (the path
   configured in `signingKey`) — do that unless you already have a release
   keystore. **Back this file up somewhere safe and never commit it** — losing
   it means you can never update the Play Store listing again. `twa/` is
   already covered by an entry in the repo's `.gitignore` for the keystore
   and build output.

   Alternatively, regenerate `twa-manifest.json` itself from scratch against
   the live manifest instead of editing the placeholder by hand:

   ```sh
   npx @bubblewrap/cli init --manifest https://<your-domain>/manifest.webmanifest
   ```

4. **Get the SHA-256 certificate fingerprint** for `assetlinks.json`:

   ```sh
   keytool -list -v -keystore android.keystore -alias android
   ```

   (Or read it from Play Console → Setup → App integrity, once the app has
   been uploaded — Play re-signs the app with its own key, so the
   Play-side fingerprint is the one that must end up in `assetlinks.json`
   for a production release.)

5. **Update `public/.well-known/assetlinks.json`** (repo root, not this
   directory — it must be served from the PWA's own origin) with the real
   `package_name` and `sha256_cert_fingerprints`, then redeploy the web app.
   Verify it with Google's
   [Digital Asset Links tool](https://developers.google.com/digital-asset-links/tools/generator)
   before publishing — a mismatch here means the TWA opens as a browser tab
   with an address bar instead of a trusted fullscreen app, and Play Billing
   won't work at all.

6. **Build the release bundle**: `npx @bubblewrap/cli build` again produces
   `app-release-bundle.aab` for upload to Play Console.

7. **Create products in Play Console** (Monetize → Products) for whatever
   gets gated — `useDigitalGoods()` can't fetch price/details for a SKU that
   doesn't exist there, and `useEntitlement()` will just never see it as owned.

### Why `twa-manifest.json` is hand-authored instead of generated by `bubblewrap init`

`bubblewrap init` fetches the manifest from a running deployment, downloads
the Android SDK/JDK on first use (can be 1GB+), and asks a battery of
interactive questions — none of which is reproducible in a scaffolding pass
before the app is deployed anywhere. `twa-manifest.json` here mirrors the
structure `init` would have produced so `bubblewrap build` (step 3 above)
can be run directly once the placeholders are filled in, or you can throw it
away and run `init` fresh against the real deployed manifest.

### Verification model

Entitlement checks trust the on-device Digital Goods API result directly;
there's no server-side purchase verification (matching the rest of the app's
local-first, no-backend design). If a gated feature ever becomes valuable
enough to be worth spoofing, add server-side verification against the Play
Developer API at that point — that would need a small backend, which this
app doesn't have today.
